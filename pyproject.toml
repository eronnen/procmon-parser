[project]
name = "procmon-parser"
dynamic = ["version"]
description = "Parser to Procmon configuration and log files formats"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Ely Ronnen", email = "elyronnen@gmail.com" },
]
dependencies = [
    "construct>=2.10.54",
]
classifiers = [
    "Intended Audience :: Developers",
    "Intended Audience :: Education",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/eronnen/procmon-parser"
Repository = "https://github.com/eronnen/procmon-parser.git"

[dependency-groups]
dev = [
    "pytest",
    "python-dateutil",
    "coverage",
    "ruff",
    "pre-commit>=4.3.0",
]

[tool.ruff]
line-length = 127

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "C4", "RUF"]
# the parsers spell out every binary case explicitly, even when branches share a body
ignore = ["SIM114"]

[tool.pytest.ini_options]
filterwarnings = ["error"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "procmon_parser/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["procmon_parser"]

[tool.hatch.build.targets.sdist]
include = [
    "procmon_parser",
    "docs",
    "tests",
    "README.md",
    "LICENSE",
]
